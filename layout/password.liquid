<!doctype html>

    <html>
        <head> 
            {{ content_for_header }}
        </head>
        <body> 
            {% section 'custom-liquid' %}
            {{ content_for_layout }}
        </body>
    </html>